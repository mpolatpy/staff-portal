import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    observationDomain: {
        // border: "1px solid",
        // borderColor: "#d3d3d3",
        // borderRadius: "5px",
        // boxShadow: "0 1px 2px 0 rgb(60 64 67 / 30%), 0 1px 3px 1px rgb(60 64 67 / 15%)",
    },
    observationDomainInner: {
        padding: '10px',
    }
}))